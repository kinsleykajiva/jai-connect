package io.github.kinsleykajiva.ai.vendors.google;

/**
 * Modes for Google LIVE API.
 */
public enum RealtimeMode {
    /**
     * Standard conversation mode (speech-to-speech).
     */
    CONVERSATION,

    /**
     * Transcription-only mode (speech-to-text).
     */
    TRANSCRIPTION
}
